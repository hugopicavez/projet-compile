program
begin
   if (true) then
        write("Succes : if(true)");
   else
        write("Echec : if(true)");
   end;
	new_line;
   if (false) then
        write("Echec : if(false)");
   else
        write("Succes : if(false) on passe dans le else");
   end;
   	new_line;
end.
